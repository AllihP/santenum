from django.apps import AppConfig

class ElearningConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'elearning'
    verbose_name = "Plateforme e-learning Santé Numérique" # Nom plus lisible dans l'admin